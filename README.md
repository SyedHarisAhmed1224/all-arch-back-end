This project uses XAMPP MySQL DB, queries are in the file initial_schema.sql
To create a dump use the bellow command in the XAMPP Shell:

mysqldump -u user -p --routines allarch > database/initial_schema.sql
